# Lumen 2 — 全维度 100% 收尾交接词（2026-08-04）

**读者：** 接手的执行 agent（"加深"执行者）。本文档是**全维度 100% 的唯一工作清单**：把 Lumen NextGen 从 `v2.0.0-rc.1`（候选版，自动化 ~88%）推到**正式 v2.0.0 且总纲各维度全闭**。
**权威文档：** 合同 `docs/LUMEN-NEXTGEN-EXECUTION-CONTRACT-2026-08-03.md`、总纲（本会话的 349KB 执行书，用户可提供）、纪律 `docs/nextgen/INVARIANTS.md`、本清单 `docs/人工门清单-2026-08-04.md`、`docs/verification-debt.md`（已全闭，**新的债务由你登记**）。
**每一条都要先验证再行动，禁止凭记忆断言（P0 优先于一切文档）。**

---


### 会话进度 2026-08-04 执行者（加深）

- **P1–P5 已关**：版本身份拆分断言；tuple docs/quotepath+docs 白名单；`ensure-release-binary.sh`；S8 对 429/5xx/Http 有界 resubmit（排除 EmptyResponse/IdleTimeout）；`context_manifest_v1_` shell≥3 + memory≥8 真测试；L4 full short **PASS**（debug/release 均可）。
- **A1 已关**：`CapabilityGrantV1` TTL/revoke token/投影（不泄密）+ 8 测。
- **A2 已关（生产路径）**：child `prepare_tool_call` 强制 `authorize_tool_dispatch`；结果 `clamp_tool_result_text`；offline `TOOL_CONTRACT_DISPATCH_GATE=PASS`。
- **A3 已关**：`BudgetLedger` reserve/settle 12 真测。
- **A4 已关（gate+fixture）**：token flood / two-tree / shutdown drain fixture + offline `FLOW_CONTROL_GATE=PASS`。
- **A5–A12 已关（Exit Gate + 生产接线）**：`nextgen_exit_gates` 7 真测；offline 17/17（含 A5–A12）；shell `validate_resume_manifest_identity`/`validate_context_rebuild_entry`→`authorize_context_rebuild`；expert repair→`authorize_expert_repair_pass`；`ConsultAdvisorHost::invoke_tool`=`lumen_advisor_consult`。DEBT-013/014 **closed**。
- **D1 已关**：`docs/evidence/reducer-purity-probe-2026-08-03.json`。**D2 已关**：DeepSeek V4 Flash + Grok 4.5 交叉验收。DEBT-016 **closed**。
- **B1**：live L0 已跑 PASS（DEEPSEEK 可用时）；**B2** L4 PASS。
- **C1 已关**：正式 signed tag `v2.0.0` → source A `d42ea6e8`，evidence B `77a81393`，已 push origin；`lumen 2.0.0 (d42ea6e8)`；DEBT-015 **closed**。
- **S8 传输层**：pre-stream cli-chat-proxy 断流 clean-seal resubmit + HTTP/1.1 逃逸（`8bd6ddba`）。
- **进程/脚本 thrash**：`source-lock` 现以 **installed binary stamp** 为准（禁止 evidence HEAD 盖写 lock）；tuple FAIL 打印恢复 hint（`6ac12372` 系）。
- **Offline gates**：18/17+A3 token reservation 真路径 `A3_TOKEN_RESERVATION_GATE`。

## 0. 一句话现状

Lumen 2 已达到 **Harness Kernel local-ready 并正式发布**：正式 signed tag `v2.0.0` → source A `d42ea6e8`（本地 binary `lumen 2.0.0 (d42ea6e8)`、tuple 绿、版本一致性 PASS）；Release Lumen workflow（run 30883143996）四平台构建中，完成后发布 GitHub Release；verification_debt 全 closed（含 DEBT-015）。S8 传输层/source-lock thrash 均已修复；grok-4.5 401 已根治。

## 1. 当前状态快照（2026-08-04 实测）

```bash
cd /Users/lei/code/lumen
git branch --show-current          # main（rc.1 已合并）；工作分支 sync/absorb-upstream-20260731
~/.local/bin/lumen --version      # lumen 2.0.0-rc.1 (29ae8804)
git ls-remote origin refs/tags/v2.0.0-rc.1   # 3f2979a7 → 29ae8804（source A）
gh pr checks 134                   # 3/3 PASS（已 merged）
SOURCE_LOCK.json                   # git_short=29ae880, lumen_version=2.0.0-rc.1
git diff --check                   # 0；工作树干净
```

## 2. 全维度 100% 目标（每项做完标 Gate）

### A. 功能切片（总纲 NG 剩余，12 项）

| # | 切片 | 现状 | 要求（Exit Gate） |
|---|------|------|-------------------|
| A1 | NG-02 CapabilityGrantV1 | Partial：只有 depth 收缩+leaf deny | grant 带 TTL/revoke token，正/反例测试，UI 投影一致 |
| A2 | NG-02A ToolContract dispatch 强制 | Partial：schema 有，dispatch 未接 | ToolContractV1 进真实 tool dispatch，result 有界投影+redaction 强制，全路径测试 |
| A3 | NG-03B token 预算预留 | Partial：token 预留额=0 | 账户级 token 预算 reserve/settle 闭环，反例（超预留拒绝） |
| A4 | NG-03E 全 mailbox 有界化 | Partial：只有 delivery_observation | token flood/two-tree fairness/shutdown drain fixture；FLOW_CONTROL_GATE=PASS |
| A5 | NG-04C compact/resume 全入口 | Partial：admission 有 | compact/resume/reconnect 经同一 manifest+rendered-input hash 重建；shell 补 `context_manifest_v1_` 真测试（当前 0 tests matched） |
| A6 | NG-04D-4 全 adapter consumer 强制 | Partial：主 writer 有 | 全 adapter/process consumer 强制 + handoff 投递 receipt 入 journal |
| A7 | NG-04E run_loop 全量接线 | Partial：三 reducer 有 | run_loop 全量接线 + workflow/Expert repair adapter + obligation 语义完整强制 |
| A8 | NG-06A 虚拟工具产品面 | Partial：consult host 有 | `lumen_advisor_consult` 注册真实 ToolRegistry；on_demand 完整（capsule/限额/取消）；UI 投影 |
| A9 | NG-07 recommend UI + root approval 全流程 | Partial：apply 门有 | recommend 界面 + root approval + 实际 model receipt 链 + budget reservation 全条件 Applied |
| A10 | NG-08 daemon/UI/ACP 接线 | Partial：纯控制面有 | lease-consumer 接真实 daemon 循环；OperatorControlPlane 五命令接 UI/ACP typed command；fake-clock exact-binary proof |
| A11 | NG-09B bounded-assignment golden | NotDone（总纲刻意不等待） | 前置 A9 后做：每条 Applied 有 root approval+sealed receipt+TreeBudget+manifest hash+model receipt+ledger decision |
| A12 | NG-10B~E release 全链 | NotDone | installer 传 ReleaseSourceTuple、provenance 落安装包、rollback receipt、NG-10C 接线 |

### B. 验证维度（真实环境）

| # | 项 | 要求 |
|---|-----|------|
| B1 | L0-L3 live 测试 | 配 DEEPSEEK_API_KEY 跑 `smoke-deepseek.sh`/`agent`/`-l2/-l3`（当前全 SKIP） |
| B2 | L4 fault-cancel 复查 | `artifacts/readiness/L4-full-cases/429/out.log` 已空；重跑确认是否真实失败（本会话一次 verify 报 exit 1） |
| B3 | 24h soak（可选，总纲排除项） | `LUMEN_L5_MODE=soak` 长跑；若做需绑定当前 binary sha |
| B4 | 真实 provider 深度会话 | 用户用 rc.1 干活（验收），执行者只补能自动化的部分 |
| B5 | Windows 专项（总纲排除） | 明确不做，除非用户改口 |

### C. 发布维度

| # | 项 | 要求 |
|---|-----|------|
| C1 | 正式 `v2.0.0` tag | rc 验收通过后 `bash scripts/release.sh 2.0.0`（两段事务已修好；tag 自动指向 source A；**先 --no-push 演练再正式**） |
| C2 | GitHub Release + 安装包 | 可选（取决于是否对外分发）；若做用 release.yml workflow |

### D. 证据/文档维度

| # | 项 | 要求 |
|---|-----|------|
| D1 | NG-00.5 Reducer Purity Probe 收据 | `artifacts/r0/reducer-purity-probe.json` 目前 gitignored 未提交——按纪律并入 evidence（或显式登记为债务） |
| D2 | S9/S12 补 grok-4.5 交叉审查 | 本会话子代理环境不可用，S9/S12 由 Director 亲自实现无独立第二模型审查——**建议补一次独立审查**（重点：operator_control.rs、kairos_lease_consumer.rs、client_advisor_consult.rs、ConsultAdvisorHost） |
| D3 | 交接词状态同步 | 每次切片完成更新本文档（或标 closed） |

## 3. 本会话问题清单（未解决 = 你的测试任务）

### 3.1 未解决（P1 优先）

| # | 问题 | 现状 | 要求 |
|---|------|------|------|
| P1 | `auto_update::tests::unstamped_update_version_matches_shipping_crate_version` 失败 | 实测 FAIL（S13 Builder 标注：上游 0.2.116 vs Lumen 2.0.0-alpha.1 拆分的有意设计，断言 `xai_grok_version::VERSION == CARGO_PKG_VERSION` 已不成立） | 修断言语义（Lumen 产品线 vs 上游协议身份分开断言）或改测试，全绿 |
| P2 | check-binary-tuple 对 docs-only 尾缀敏感 | HEAD 含 docs commit 时 FAIL（"binary not built from expected HEAD"）；docs-only 按纪律不刷锁 | 让 tuple 检查接受 docs-only 尾缀（参考 install-local 的 evidence-suffix 逻辑）或明确定义 |
| P3 | verify-readiness 与 evidence-suffix 冲突 | L4/L5 脚本在 HEAD 上 `cargo build --release` 覆盖 release binary（HEAD=evidence commit 时 stamp 错位 → binary_tuple_post FAIL） | 统一语义：verify-readiness 应在源码 HEAD 跑，或 L4/L5 用 LUMEN_RELEASE_BIN 指向 locked build |
| P4 | L4_fault_cancel 429 失败 | out.log 已空，无法确认 | 重跑 `smoke-deepseek-l4.sh` 定位（本地 fault server 场景） |
| P5 | `context_manifest_v1_` 0 tests matched | shell 侧缺真测试（评估发现） | 补真实入口测试（A5 一并） |

### 3.2 已解决（防止重踩，不用修）

- grok-4.5 经 cli-chat-proxy 大上下文传输断 ×4 → 轻量化上下文/重试；网络恢复即好（**坑：给 grok-4.5 的任务控制上下文体积**）
- **后台 subagent 在本环境 idle 时被清空、前台 spawn 偶发 admission closed** → 长任务用前台/自执行；子代理结果不可依赖（重要环境坑）
- gitleaks 1 leak（测试夹具 `sk-` 字符串）→ 运行时拼接 + fingerprint 进 .gitleaksignore（**新增夹具 secret 形状时先检查 .gitleaksignore**）
- 磁盘 99% 满（agent/target 217GB）→ cargo clean 释放 273GB（**定期清理 target；`df -h /` 剩 <1GB 时先清**）
- CI Pager voice 测试并行 env 泄漏 → `#[serial_test::serial(VOICE_MODE)]` + EnvGuard（**凡进程级 env 测试必须 serial**）
- zsh 变量不分词 → bash 循环/脚本用 bash 执行（**`for x in "a b"` 在 zsh 下不按空格分词**）
- grok-4.5 401（no auth context）→ 用户重新登录 x.ai 后验证根治（auth.json 新 token 被代理接受）

## 4. 硬纪律（违反即回退，沿用交接词 §5）

1. 证据优先于断言；CI 未跑写 `NOT RUN`。
2. **SOURCE_LOCK 顺序不可交换**：源码 commit → lock → evidence commit → push。
3. 不发明完成：Progress ≠ claim；模型说 done ≠ done。
4. fail-closed 默认：不确定 → `RecoveryRequired`/`Frozen`，禁止自动 retry/replay。
5. 不扩大成不可收敛重写；每片有边界。
6. 测试驱动真实入口：禁止 mock-only UI、0 tests matched。
7. 版本身份边界：产品版本只看 VERSION/`lumen --version`；上游 0.2.116 只做协议身份。
8. **新债务登记进 `docs/verification-debt.md`（当前全 closed，你引入的新债务必须显式登记）**。

## 5. 环境与命令

```bash
export PATH="$HOME/.local/bin:$HOME/.cargo/bin:/opt/homebrew/bin:$PATH"
export PROTOC="${PROTOC:-/opt/homebrew/bin/protoc}"
cd /Users/lei/code/lumen/agent
# 测试一律串行（worktree_pool 并行会抖动）
cargo test -p xai-grok-memory --lib <filter> -- --test-threads=1
cargo test -p xai-grok-shell --lib <filter> -- --test-threads=1
cargo test -p xai-grok-pager --lib -- --test-threads=1
cd /Users/lei/code/lumen
bash scripts/run-nextgen-offline-gates.sh      # 离线 gate（receipt 写 SCRATCH/）
bash scripts/test-release-order.sh             # release 顺序回归（S13）
bash scripts/check-verification-debt.sh        # 债务 read model（目标保持 0）
~/.local/bin/gitleaks detect --source . --no-banner   # 必须 no leaks
bash scripts/check-binary-tuple.sh             # binary tuple（注意 P2 docs 尾缀）
# 三段式（发布前）：checkout <source> && cargo build -p xai-grok-pager-bin --release && checkout 回 && LUMEN_SKIP_BUILD=1 bash scripts/install-local.sh
```

## 6. 完成定义（100% 的机器可判定清单）

```text
A1–A12 全部 Exit Gate PASS
＋ B1/B2 通过（B3 可选、B5 不做除非改口）
＋ C1 正式 v2.0.0 tag 指向 clean source A（release.sh 两段事务）
＋ P1–P5 全部解决（或显式登记债务+理由）
＋ verification_debt == 0（含新登记项全部关闭）
＋ CI 对新 HEAD 3/3 全绿（记录 run id）
＋ 全量回归 battery（15 套件）+ offline gates + gitleaks + tuple 全绿
＋ 【INV-36，2026-08-04 收尾审计增补】readiness gate：`CURRENT_STATE_LEDGER.md`/`artifacts/readiness/status.json` 报告 `ready=true` 且 `state=READY`（`release_version_changed` 类自动 blocker 必须先重跑 `verify-readiness.sh`；M5 真人陌生测试、M6 15 天真实日志、live eval 是人工门，SKIP 不视为 ready）
→ 才可称"全维度 100%"。仍不宣称 Windows/live 无人工证明。
```

**收尾检查单（每次会话结束）**：`git status -sb` 干净 / 已 push / SOURCE_LOCK 与 HEAD 一致或 docs-only / 本机 binary 与 lock 一致 / 本文档更新 / 无未声明的红。

## 2026-08-04 收尾审计落地（本目标）

- **身份层**：`NodeIdentityV1`/`GrantRevisionV1`/`AttemptContextV1`/`GovernedRunEnvelopeV1` 四 DTO + `DispatchPermitV1` 线性链（crate-private 字段、`PermitConsumer` 注册表、dispatch 时重验）+ `RootBypassPermission`（INV-5 全字段）—— `identity_envelope.rs`/`dispatch_permit.rs`/`root_bypass.rs`，28 测；离线 gate `IDENTITY_ENVELOPE_GATE`/`DISPATCH_PERMIT_GATE`/`ROOT_BYPASS_GATE` PASS。
- **红队合同**：`SecretRef`/redaction fail-closed（`secret_ref.rs`）；claim 状态机补 `EvidenceAttached`/`Conflicted`/`Inconclusive`/`Frozen` 四变体 + 转移规则（作者 attach、root 唯一 review/freeze、conflict 新记录解析）；typed 三 profile 一次性不可降级（`runtime_profile.rs`）；`artifacts/audit/latest.json` 由 offline-gates 脚本生成（`audit_snapshot.rs` 读写 fail-closed）。新 gate：`SECRET_REF_GATE`/`CLAIM_STATE_MACHINE_GATE`/`RUNTIME_PROFILE_GATE`/`AUDIT_SNAPSHOT_GATE`。
- **接线**：A4 生产 prompt mailbox 有界化（128 上限，DroppedFull 显式拒绝 + QueuePressure 投影）；A6 child terminal → handoff receipt 入 task-tree-lifecycle JSONL（`on_completed` 生产路径）；S3 child `git commit/push/merge` 硬拒（`prepare_tool_call` Execute 工具 child depth，端到端测试）；A10 `SchedulerLoopOutbox` outbox 幂等门接真实调度 dispatch（成功 terminal 才标记，失败可重试）；A12 `ReleaseSourceTupleV1` + release.sh provenance + install-local 落安装目录；M1 `x.ai/governedTree/status` ACP 投影（离线三节点树 + 升级面）。
- **readiness**：`release_version_changed` blocker 已消除（对 f51fb902 重跑聚合），`binary_sha256` 绑定当前 build；1h soak 已对锁定 binary 执行；M5/M6/live eval 人工门仍 open（DEBT-024(e)，绝不伪造）。
- **离线 gates**：18 → **26 全 PASS**（receipt `SCRATCH/nextgen-offline-gates/offline-contract-gates-full.json`）。

## 2026-08-04 收尾审计最终状态（push 后）

- **锁定链**：source `9a133a95`（identity/red-team/wiring 全量 + tuple allowlist 修复）→ SOURCE_LOCK 9a133a95 → binary `lumen 2.0.0 (9a133a95)`（sha 4de5d07d…）→ 1h soak（3600s/117 turns，绑定该 binary）→ readiness 聚合。
- **readiness 终态**（`artifacts/readiness/status.json`，脚本生成、未手改）：`state=BLOCKED ready=False can_tool_call=True engineering_complete=False`。**所有自动化 gate PASS**：binary_tuple_pre/post、gitleaks（0 leak）、defaults、security、m2、parity、eval_harness、verify_cli、verticals、L0_connect、L1–L3（live DeepSeek）、L4_fault_cancel、L5_long_session、**L5_one_hour_soak**、R0_full、sbom、legal、eval_live（20/20）、source_lock。唯一 auto_blocker 是 `reconcile`，其唯一成因是 `m5_onboarding_semantics_invalid`（M5 模拟证据语义非法）；publish blockers 仅剩 **M5_10_min_stranger**（真人 10 分钟陌生测试）与 **M6_15_day_self_use**（15 天真实日志，当前 2 天）。这两个是脚本语义拒绝伪造的人工门（INV-36：SKIP/模拟不视为 ready）。
- **CI**：`ci.yml`（cargo tests + offline gates + clippy）对证据 HEAD **success**（run 30921300143）；`windows-team-build.yml` 为**既有红**（今日每笔提交均 link.exe 4319 失败，含本目标之前的 3 笔；总纲 Non-goals 排除 Windows 专项）。
- **环境修复（非代码）**：会话中途本机 homebrew python@3.14 被外部卸载（Cellar 清空、pytest shebang 悬空、py3.9 下 `X | None` 运行时 TypeError 击穿 reconcile）→ `brew install python@3.14` 恢复，verify_cli/reconcile 随之全绿。
- **证据**：`docs/evidence/offline-contract-gates-full.json`（26/26）、`artifacts/audit/latest.json`（生成式审计快照，禁手改）、`docs/verification-debt.md`（DEBT-017..023 closed，DEBT-024 残余 partial 登记）、`docs/nextgen/INVARIANTS.md`（INV-36）。

## 正式发布（2026-08-04 收口）

- **v2.0.0 GitHub Release 已发布**：https://github.com/exergyleizhou-ux/lumen/releases/tag/v2.0.0
- 四平台二进制 + minisig + SPDX SBOM + manifest + 公钥共 19 assets；immutable、非 draft、非 prerelease。
- workflow run 30891372326（1h31m）全绿；SBOM 官方校验通过（MPL-2.0+ 规范化修复）。
