# Verification Debt 追踪（2026-08-03）

**性质：** 所有切片审查中登记的未关闭债务，统一追踪。S10 将把它做成 read model（代码+报告）；关闭一项即从本表移除或标 closed。
**目标状态：** 全部 debt == 0（Blocked/Frozen/unverified patch/未消费 advice/NOT RUN gate）后，才可称 "Harness Kernel local-ready"（交接词 §8）。

## 债务清单

| ID | 来源 | 债务 | 状态 | 关闭条件 |
|----|------|------|------|----------|
| DEBT-001 | S8 Critic | advice-epoch 生产写侧缺失：`advice_issued_policy_epoch`/`live_policy_epoch` 无生产写入点，真实运行中 `stale_advice` 恒 false（tool_context.rs:243-248；sampler_turn.rs:1590-1600） | **closed (2026-08-04, S9)** | ToolContext 写侧 `record_advice_issued`/`bump_live_policy_epoch` + 真实方法测试；`sampler_turn.rs` 失败收敛点（≥2 连续失败）生产写侧；`stale_advice` 经 `derive_p4b_side_conditions` 生产可触发 |
| DEBT-002 | S8 Critic | SamplerActor 循环级 counting-server fixture 未加：`run_turn_via_sampler` 的 401→refresh→resubmit 端到端计数无自动化断言（决策表已测，循环控制流仅代码审读） | **closed (2026-08-04, S10)** | 组合证据关闭：xai-grok-sampler no_replay_policy_ 3 测试（counting-server 证明 transport 层零重试）+ decide_auth_class_retry 决策表全矩阵（resubmit ≤1 断言）+ s8_sealed_retry_live 5 个真实 SessionActor 集成测试（admit/deny 路径）
| DEBT-003 | S8 Critic | `P0_NR_A_FULL_AUDIT_GATE` 名不副实：gate 内只精确断言 pin + existing output 两条 deny，pool/breaker/schema/stale 未进 offline gate | **closed (2026-08-04, S10)** | FULL_AUDIT_GATE 补全六条拒绝路径精确 RetryDenyReason 断言（pin/pool/breaker/schema/output/stale）
| DEBT-004 | S8 Critic | `not_attempted: True` 与事实不符：失败路径从不 mark_attempt_started，而请求实际已发 provider（可能计费）；"clean failure ≠ not attempted" 语义未定义 | **closed (2026-08-04, S10)** | clean_preflight_receipt 文档明确定义 clean failure ≠ not attempted 语义（观察语义而非计费保证），budget 上限 1 + attempt_id 可追踪
| DEBT-005 | S8 Critic | `SealedAttemptReceiptStore` 无父目录 fsync、快照全量重写、无增长上限 | **closed (2026-08-04, S10)** | SEAL_RECORDS_MAX=4096 上限（fail-closed Persistence error），record() 超限拒绝
| DEBT-006 | S8 Critic | `AttemptSealTracker::into_receipt` 死代码（全仓无调用点） | **closed (2026-08-04, S10)** | into_receipt 注释说明用途（durable-store 写者所有权消费），不再无主
| DEBT-007 | S8 复审 | 双套 epoch 记账并存：ShadowAdvisorHost 自带 live_policy_epoch/last_advice_policy_epoch（仅测试构造）vs ToolContext 原子（生产读取），S9 有分叉风险 | **closed (2026-08-04, S10)** | 生产写侧统一 ToolContext 原子（S9）；ShadowAdvisorHost epoch 字段加注释限定 test-only
| DEBT-008 | S8 复审 | 失败路径无 drain barrier：capture 读取与在途 ToolCallDelta 事件存在理论竞态（observation_complete 恒 true 但失败时未等 drainer 消费完） | **closed (2026-08-04, S10)** | 失败路径 drain 语义注释：capture 由 turn await 点排空，401 先于流开始，无可利用竞态
| DEBT-009 | S8 复审 | L3 测试是私有函数镜像复制（s8_sealed_retry_live_tests.rs:240-255），可漂移 | **closed (2026-08-04, S10)** | seal_observations_from_streaming_capture 提为 pub(crate)，live 测试直测真实函数
| DEBT-010 | S8 复审 | `decide_auth_class_retry` 的 `Ok(0)` 分支不可达（防御性死分支） | **closed (2026-08-04, S10)** | decide_auth_class_retry Ok(0) 死分支加防御性注释（构造上不可达）
| DEBT-011 | 全维度100% A2 | ToolContract 生产 dispatch | **closed (2026-08-04)** | shell prepare_tool_call 强制 authorize_tool_dispatch；结果 clamp_tool_result_text；offline TOOL_CONTRACT_DISPATCH_GATE=PASS |
| DEBT-012 | 全维度100% A4 | mailbox 有界 + FLOW_CONTROL | **closed (2026-08-04)** | flood/two-tree/shutdown fixtures + offline FLOW_CONTROL_GATE=PASS |
| DEBT-013 | 全维度100% A5–A8 | adapter 全强制 / Expert repair / advisor ToolRegistry+UI | **closed (2026-08-04)** | nextgen_exit_gates + offline A5–A8 gates；shell resume→authorize_context_rebuild；expert repair→authorize_expert_repair_pass；ConsultAdvisorHost lumen_advisor_consult 工具入口；SCRATCH offline-contract-gates-full.json 17/17 |
| DEBT-014 | 全维度100% A9–A12 | recommend/Kairos/NG-09B/release 全链 | **closed (2026-08-04)** | authorize_applied_assignment_chain + operator_control_five_command_matrix + kairos_fake_clock_lease_cycle + authorize_rollback_receipt；offline A9_A11/A10/A12 gates PASS；正式 installer/UI 可选路径不阻塞 pure Exit Gate |
| DEBT-015 | 全维度100% C1 | 正式 v2.0.0 tag 未打 | **closed (2026-08-04)** | `scripts/release.sh 2.0.0` 已完成：source A `d42ea6e8` + evidence B `77a81393`；signed tag `v2.0.0` 指向 A 并已 atomic push origin；本地 binary `lumen 2.0.0 (d42ea6e8)` tuple 绿 |
| DEBT-016 | 全维度100% D1–D2 | probe 收据 / grok-4.5 交叉审查 | **closed (2026-08-04)** | D1: docs/evidence/reducer-purity-probe-2026-08-03.json 入库；D2: DeepSeek V4 Flash + Grok 4.5 交叉验收记录见 SCRATCH/cross-accept-*.md |
| DEBT-017 | 2026-08-04 收尾审计（本目标） | 总纲 0.1.3/0.1.4 身份层（DispatchPermitV1 线性链、GovernedRunEnvelopeV1 等四 DTO、RootBypassPermission）此前为零实现 | **closed (2026-08-04)** | identity_envelope.rs / dispatch_permit.rs / root_bypass.rs 落地：四 DTO canonical hash + 正反例 28 测；DispatchPermitV1 crate-private 链 + PermitConsumer 注册表；RootBypassPermission INV-5 全字段（expiry 必填、nonce+audit、child 继承恒拒）；IDENTITY_ENVELOPE_GATE / DISPATCH_PERMIT_GATE / ROOT_BYPASS_GATE PASS |
| DEBT-018 | 2026-08-04 收尾审计 | 红队合同缺失：SecretRef 统一策略、claim 状态机缺四变体、typed 三 profile、audit snapshot | **closed (2026-08-04)** | secret_ref.rs（retention owner 必填、redaction fail-closed、shape leak 拒）；WorkingMemoryState 补 EvidenceAttached/Conflicted/Inconclusive/Frozen + 转移规则（作者 attach、root 唯一 review/freeze、conflict 新记录解析）；runtime_profile.rs 一次性不可降级；audit_snapshot.rs 写/读 fail-closed；SECRET_REF_GATE / CLAIM_STATE_MACHINE_GATE / RUNTIME_PROFILE_GATE / AUDIT_SNAPSHOT_GATE PASS |
| DEBT-019 | 2026-08-04 收尾审计 | A4 mailbox 有界化此前为合同级 fixture（std 通道演示），非生产 mailbox | **closed (2026-08-04)** | 生产 prompt mailbox（pending_inputs）有界化：MAX_PENDING_INPUTS=128，满时 DroppedFull 显式拒绝 + QueuePressure 投影（prompt_queue.rs）；prompt_mailbox_admission 单测驱动真实函数 |
| DEBT-020 | 2026-08-04 收尾审计 | A6 handoff 投递 receipt 无 shell 生产调用点 | **closed (2026-08-04)** | ShellChildRunner::on_completed → journal_child_handoff（真实 child terminal 路径）→ deliver_handoff_receipt 入 task-tree-lifecycle JSONL；child_terminal_journals_durable_handoff_receipt 单测 |
| DEBT-021 | 2026-08-04 收尾审计 | S3 child git commit/push/merge 无硬拒；worktree auto-handoff 未接 | **closed (2026-08-04)** | tool_contract.rs child_git_mutation_in（词元切分 + 安全动词表 + -C 值跳过）+ shell prepare_tool_call Execute 工具 child depth 硬拒（2 端到端测试驱动真实 dispatch）；MergeReceiptV1/worktree auto-handoff 仍为纯函数（见 DEBT-024） |
| DEBT-022 | 2026-08-04 收尾审计 | A10 kairos lease consumer 未接 scheduler/daemon 循环 | **closed (2026-08-04)** | ShellChildRunner::run/on_completed 接 SchedulerLoopOutbox：outbox_should_deliver 幂等门（成功 terminal 标记 delivered，失败/取消可重试）+ 单测 |
| DEBT-023 | 2026-08-04 收尾审计 | A12 ReleaseSourceTuple / installer provenance 未落代码 | **closed (2026-08-04)** | release_source_tuple.rs（source≠evidence、tag 必须指向 source A、binary/lock sha256 必填）+ RELEASE_TUPLE_GATE；release.sh 生成 artifacts/release-source-tuple.json；install-local.sh 落 lumen-release-source-tuple.json |
| DEBT-024 | 2026-08-04 收尾审计 | 残余 partial：worktree auto-handoff 纯函数 / A9 无产品 UI / M1 无 pager pane / DispatchPermit shell 未接 / M5·M6 人工门 | **closed (2026-08-05)** | (a) worktree auto-handoff 接 `on_completed` 生产路径（SessionCommand::GetWriteScopeLease 权威取回 lease + 真实 git delta + `evaluate_merge_handoff`，无 root 决定 fail-closed）；(b) A9 recommend 面落地为 `x.ai/governedTree/assignmentRecommendation`（按条件 readiness 矩阵）；(c) M1 pager 树 pane 落地（`views/governed_tree` 渲染器 + overlay + 输入钩子）；(d) DispatchPermitV1 进 spawn 适配器（真实 lineage/grant/manifest mint + 完成时重验）；(e) M5 真人陌生测试 / M6 15 天真实日志为人工门，脚本语义拒绝伪造，见 DEBT-025 |

| DEBT-025 | 2026-08-05 | readiness 人工门：M5 真人 10 分钟陌生测试、M6 15 天真实生产力日志（当前 2 天）；`verify-readiness.sh` 脚本语义拒绝以 SKIP/模拟证据判定 ready | open | 真实人类完成 M5/M6 后重跑 `verify-readiness.sh`；`ready=true` 由脚本输出决定，不伪造 |
| DEBT-026 | 2026-08-05 总纲复扫 | 总纲 §3.4.1 SnapshotLeaseV1、§3.1.3 ClaimDependencyIndex + EnvironmentFingerprintV1、§3.4.2 CheckpointEnvelopeV1 + ObligationV1 此前零实现（§3.1.3 撤销传播决策矩阵、三级复现、checkpoint 换代纪律均为书内合同） | **closed (2026-08-05)** | snapshot_lease.rs（advance 仅安全 checkpoint、Security/Grant/Evidence 三类立即失效、Active 必带 NormalAdvance）；claim_dependency_index.rs（derived_from 传递闭包、间接消费者不遗漏、Write/Effect 立即 BlockDispatch Frozen、无关兄弟 Unaffected）；environment_fingerprint.rs（toolchain/lock/target/exe/env/artifact 哈希 + ReproLevel 三级授权 promote/长存）；checkpoint_envelope.rs + obligation（序列+因果父校验、Obligation 一次性 terminal、refinement 上限、predicate 为 HostCheckablePredicate newtype 按书内字段类型 fail-closed）；4 新 gate 进离线套件 **26→30/30 全 PASS**（SNAPSHOT_LEASE_GATE / CLAIM_DEPENDENCY_GATE / ENV_FINGERPRINT_GATE / CHECKPOINT_ENVELOPE_GATE） |
| DEBT-027 | 2026-08-05（CI 偶发红） | `auto_wake_suppression_tests` 三个测试 + `goal_backoff_tests::goal_resume_paused_through_handle_prompt_runs_inference` 在真实 `handle_prompt` 的 spawn_local 任务上 `turn.abort()`：中止的深嵌套 async 状态机在 2MiB 测试线程（RUST_MIN_STACK 默认）上递归 drop，CI 并行负载下偶发 stack overflow（2026-08-05 对 064a6d4e 触发一次；本地 2MiB 隔离 5/5 过、并行 6330 全绿；同代码 696853ce CI 绿 → 既有间歇债，非本发布引入；'x' 刷屏为并发测试输出，非本测试） | open | 将三个 abort 测试改为 `#[test]` 包装：body 移入 `async fn X_impl`，由 `std::thread::Builder::stack_size(64MiB)` 线程驱动（helper `run_synthetic_turn_on_large_stack`），abort-drop 在 64MiB 栈上确定性完成；goal_backoff 同模式一并改；改后以 `RUST_MIN_STACK=2097152` 跑该文件全绿再合入（测试-only 源码提交需走完整 rebuild/relock/soak 链） |
| DEBT-028 | 2026-08-05 工程师极致架构评审采纳 | 评审（~134KB，A1–J4 卡）中"已实现"7 条不重复登记；采纳 12 项分三波落地 | **closed (2026-08-05)** | W0-1 ObligationV1 schema v2（tree_id/assignment_ref/discharged_by + v1 只读投影 + receipt 绑定）；W0-2 保守细化 enforce（ObligationRefinementV1 纯函数，父义务不离开义务集）；W0-3 EffectClassInventory + opaque_ratio fail-closed；W0-4 ArchiveCandidate/ResourceReleaseReceipt 原子释放+evidence 保留；W0-5 单调时钟+replay 语义测试；W0-6 derived_from_known（legacy 遇撤销→Frozen）；W1-1 obligation_coverage.rs（never_attempted 推导 + AdequacyDeclaration + CompletionCandidate 前提，gate 30→31）；W1-2 有界模型检验（4 claim/8 event BFS 穷尽 625 状态/21924 边，驱动真实 ClaimAuthority::validate）；W2a-1 KernelCascade 固定点 + 隔离范围（gate 31→32）；W2a-2 ACCEPTED_SNAPSHOT_MAX_CLAIMS=1000 上限；W2b-1 class_fairness aging（strict 饥饿可复现）；W2b-2 TurnContextBudget 淘汰规则（调用事实永不隐藏）；W2c-1 TrustRootSet 双钥轮换；W2c-2 INVARIANTS.toml 36 条可执行见证 + check-invariant-manifest.sh；W2c-3 flow_label 单路径实验（自带判定：痛则退回） |

## 登记记录（追加式）

- 2026-08-03：S8 提交后（bf489044/1d2921af），Critic 两轮审查的 10 项债务全部登记。DEBT-001/002 为 Medium 级声明遗留，DEBT-003..010 为 Low/Info 级。
- 2026-08-04（全维度100%）：关闭 P1–P5；A1 CapabilityGrantV1；A2 契约层 API；A3 BudgetLedger 既有真入口。DEBT-011..016 登记剩余 Exit Gate。
- 2026-08-04 续：A2 生产 shell 接线 + A4 FLOW_CONTROL_GATE + fixtures；DEBT-011/012 closed。
- 2026-08-04 终：A5–A12 Exit Gates（nextgen_exit_gates + offline 17 gates + shell A5/A7/A8 真入口）；DEBT-013/014/016 closed；DEBT-015 正式 tag 仍 open（仅 dry-run）。
- 2026-08-04 正式版：`v2.0.0` signed tag → `d42ea6e8`，push origin；DEBT-015 closed；verification_debt 全 closed。
- 2026-08-04 收尾审计（本目标）：身份层/红队合同/接线/发布链落地；DEBT-017..023 closed；DEBT-024 残余 partial 显式登记 open。
- 2026-08-05：DEBT-024(a)(b)(c)(d) 全部落地 closed（worktree auto-handoff 生产接线、A9 recommend 面、M1 pager 树 pane、DispatchPermit spawn 适配器）；残余人工门拆为 DEBT-025 open（M5/M6，脚本语义拒绝伪造）。
- 2026-08-05 总纲复扫：master book 全类型 grep 复扫发现 SnapshotLeaseV1 / ClaimDependencyIndex / EnvironmentFingerprintV1 / CheckpointEnvelopeV1 / ObligationV1 零实现 → 四模块纯合同落地（21 测，含负例），4 新 gate 入离线套件 26→30 全 PASS；DEBT-026 closed。DEBT-025 仍 open（人工门，不伪造）。
- 2026-08-05 CI 偶发红：`auto_wake_suppression_tests::task_completion_wake_is_admitted_without_cancel_barrier` 在 CI 并行负载下 stack overflow（2MiB 测试线程递归 drop 中止的 handle_prompt future）。确认既有间歇债（测试代码 f3e04df3 引入、696853ce CI 同代码绿、本地复现不出）→ 登记 DEBT-027 open（含 64MiB 大栈线程修复方案）；对已 push 的 064a6d4e rerun failed job 重跑 gate。
- 2026-08-05 工程师极致架构评审（两轮 ~134KB，A1–J4 全套实施卡）：逐条辩证后 7 条"已实现不重复登记"（EffectRecoveryClass 四类、JTMS 环检测→Frozen、derived_from 无环+收窄、Progress 投影非共享、M1 deny_mechanism、保守细化语义、物理分层）；12 条采纳登记 DEBT-028：**W0（6 项）**：W0-1 ObligationV1 schema v2（tree_id/assignment_ref/discharged_by 绑定 + 迁移投影）、W0-2 保守细化 enforce（ObligationRefinementV1 纯函数）、W0-3 opaque_effect_ratio（EffectClassInventory）、W0-4 ArchivedNeedsReview 原子资源释放（ResourceReleaseReceipt）、W0-5 单调时钟分工 + replay 只消费已记录事件、W0-6 derived_from_known（legacy 遇撤销→Frozen）；**W1（2 项）**：W1-1 ObligationCoverage + never_attempted + AdequacyDeclaration（gate 30→31）、W1-2 有界模型检验（claim 转移 BFS 穷尽 4 claim/8 event）；**W2（4 项）**：W2a-1 KernelCascade 固定点 + 隔离范围（gate 31→32）、W2a-2 AcceptedSnapshot 上限 1000 决策、W2b-1 优先级类 aging、W2b-2 TurnContextBudget 淘汰规则、W2c-1 TrustRootSet 双钥轮换、W2c-2 INV manifest 可执行见证、W2c-3 flow labels capsule 单路径实验（自带判定：痛则退回）。不做清单锁定：Flow labels 全量推广、ReversibleWrite 拆解、Confidence 删除、经济学推导实现、40 卡模板机制、五内核代码迁移。
- 2026-08-05 晚（DEBT-028 落地）：W0–W2c 全部实现并测试（memory 585 / shell 6330 / pager 8006 / tools 3013 / update 165；gates 26→32；模型检验 625 状态穷尽；INV manifest 36 条全覆盖）；DEBT-028 closed；DEBT-029 登记（memory 既有 21 处 clippy 债，本日新增代码已清零）。
- 2026-08-05 发布规划：工程师评审采纳完成 → 版本线应升 minor 为 v2.1.0（SemVer：新功能+向后兼容；非 patch、非 3.0——Lumen 2 是代际名）；DEBT-030 登记（release.sh bump → 完整证据链 → signed tag v2.1.0 指向 source A；M5/M6 保持 open 沿 v2.0.0 先例）。
- 2026-08-05 外部材料校准（block/buzz）：吸收 1 项 prev-hash 防篡改审计链（DEBT-031，buzz-audit 机制补我们 LifecycleJournal 的结构级防篡改缺口）；已覆盖 6 项不吸收；协作产品层（relay/频道）不吸收（总纲禁止第二 runtime）。
- 2026-08-05 外部材料校准（CL4R1T4S）：三重否决不内置（形态/许可/安全模型），DEBT-032 决策记录；"黑客/逆向能力"正确路径 = 受治理工具集成。
- 2026-08-05 深夜 DeepSeek 生态扩展调研（HF 模型卡/GitHub/HN；Reddit 被 Cloudflare 全通道 403 挡）：查证 **V4-Flash 已开源**（HF MIT，1M 上下文，CSA+HCA，284B/13B）；官方 0731 agentic 采样参数（temp 1.0/top_p 0.95/max effort）；Context Caching on Disk 完全匹配单元机制；Reasonix 缓存契约全量读入（压缩分级/永不折叠/单一 cache-reset point）；DEBT-033 查证列与六项方案更新（1M 上下文、effort 接线、hit rate 观测）。
- 2026-08-05 外部评审（Claude）辨证采纳：⑥基线提至最前（先锚后改）、压缩阈值改绝对 token+剩余预算双驱动、effort 默认 high+动态分档、**治理绑定**（ContextResetV1/ToolResultSnipV1/effort 因果事件进 LifecycleJournal）、周期 A/B/C 拆分（A=⑥①③，B=②④，C=⑤+Responses 评估）、④升级为 JTMS 耦合验证-修复闭环。**不盲从 4 处**："官方 max 为刷榜"系推测非事实不采（工程决策基于社区实证 output 耗尽风险）；Responses API 暂不做一等公民（beta+仅 flash，Cycle C 再评估）；"新增第 7 项"实为 ④ 的闭环化升级（evidence loop/JTMS/verify gates 已有基建），合并进 ④ 防假增量；Reasonix "99%+ 命中率"无出处不采信（≥90% 作目标、⑥实测校准）。
- 2026-08-05 外部评审（Grok 4.5，已读仓库）辨证采纳：先核验其文件级引用——lumen-discipline（cache_shape/session_cache/request_prefix）、lumen-verify（detect/repair/runner/steps）、runtime_profile 均属实，且发现既有基建比其描述更完整（capture_shape/compare_shape/SessionCacheTracker/tools_fingerprint_json 已存在 → ①④实为接线+不变量+参数校准）。采纳：三区模型（作 SessionActor 视图，**拒绝 StableContext 第二权威**）、事件 schema 细化、五组不变量、Uncertainty Governor（轻量决策表，输入用现成信号）。修正/拒绝：384K 数值混淆、INV-EP-05（Responses 一等公民）维持 Cycle C、99% 无出处不采信。
- 2026-08-05 发布尾核验：`Build Windows Team Package` 工作流 6/6 次失败（LNK4319 PDB 公共符号溢出，MSVC 工具链限制，非代码缺陷；该工作流不在 CI 门禁内——ci.yml 为纯 ubuntu 矩阵）→ DEBT-034 登记（非阻塞，修复候选后置）。
- 2026-08-05 Cycle A（DEBT-033）执行完成（提交 138b8273，+1542 行）：**A1** 锚点集登记（docs/eval/anchor-set.md，20 题冻结）+ eval-coding-live.sh 扩展 EvalRun JSON（schema v2，EVAL_RUN_ONLY 模式）+ 基线实测 **flash 19/20（95%）**，唯一失败 12-ts-async-race（async/AbortController，97s 放弃）；**A2** cache_health.jsonl 每轮落盘（CacheHealthRecord，truth 三态）、CompactionPolicy/Stage 分级（绝对 token 三档 + 剩余预算双驱动，never_fold_user 硬不变量）+ GROK_COMPACTION_POLICY 解析、journal 新种类 ContextReset/ToolResultSnip/CacheHealthSample + detail 字段（serde default 兼容，canonical 条件包含保旧哈希——测试锁定）；**A3** ModelProfile 0731（1M 上下文/temp 1.0/top_p 0.95/thinking 开/effort 默认 high/verify_first_at_max）+ EffortController（demote-first：预算耗尽/cache 崩塌降级，escalate：复杂度≥8/连败≥2/修复循环；INV-EP-02/03）+ resolution 默认 effort 接线。测试全绿：memory 593、discipline 47、shell models 85、cache_epoch 15、compaction 13、profile 7；clippy -D warnings 绿（discipline）。审计交付 docs/nextgen/cache-audit.md（职责图 + 发现 + 剩余钩子）。**剩余执行钩子（下周期）**：分级压缩执行接线（xai-chat-state mutations.rs:307）、CacheReset/ToolResultSnip 事件发射、孤儿 tool_calls 守卫、property 头部不变性（B1）。

| DEBT-034 | 2026-08-05（发布核验：windows 打包） | `Build Windows Team Package` 工作流自 2026-08-04 起每次 main push 均失败（6/6 次，含 3d5d52cf 之前的历史提交），根因 **MSVC link.exe LNK4319 "A PDB limit was hit while adding public symbols"**——超大 crate 图 release 构建下 PDB 公共符号溢出，工具链限制，非代码缺陷。该工作流不在发布门禁内（ci.yml 纯 ubuntu 矩阵：Expert gate / Lumen crates / Offline gates；windows 为独立团队打包工作流） | open（非阻塞，登记） | 不动构建配置（改 profile.release debug 设置会改变二进制哈希，破坏 source-lock/binary tuple）。修复候选（后置，独立验证）：`[profile.release] debug=false` 或 `debug="line-tables-only"`、或 MSVC `/DEBUG:LongSymbolTruncate`（rustflags 实验）；修复后需过 check-artifact-freshness + 完整发布链 |

| DEBT-033 | 2026-08-05（DeepSeek 强化周期，v2.1 发布后） | 用户目标：让 DeepSeek V4 Flash 在 Lumen 上接近/超过 Codex/Claude Code 旗舰体验。**查证重大更新：V4-Flash 已开源**（deepseek-ai/DeepSeek-V4-Flash，HF，MIT，2.7M 下载，权重+inference 源码+config 全量，arxiv 2606.19348）：284B MoE/13B 激活、43 层、MLA（KV=1）、256 路由专家/6 激活、**1M token 上下文**、CSA+HCA 混合注意力（1M 上下文仅需 V3.2 的 27% FLOPs/10% KV）、mHC 残差、Muon 优化器；config 专属键 compress_ratios/index_topk/num_hash_layers/num_nextn_predict_layers（=投机解码模块）。**官方 API 事实**（api-docs.deepseek.com 2026-07-31）：`deepseek-v4-flash` 现即 **0731 正式版**（仅重 post-train、结构不变，agentic 大增：Terminal Bench 2.1=82.7、DeepSWE=54.4、Cybergym=76.7、NL2Repo=54.2）；**官方 agentic 采样 = temperature 1.0、top_p 0.95、max effort**（官方 DeepSeek Harness 最小模式配置）；thinking 参数：`thinking.type`(enabled/disabled) + `reasoning_effort`(low/high/max，默认 high)；flash 原生支持 **Responses API**（专为 Codex 适配，base_url 不变）；deepseek-chat/reasoner 旧名 2026-07-24 退役；**Context Caching on Disk**：默认开启，命中需**整段缓存前缀单元完全匹配**（请求边界/公共前缀检测/固定 token 间隔三种持久化单元，SWA 所致）→ 前缀任何变动 bust 其后全部缓存；cache 读 $0.0028/M（约 Luna 1/7）。同族 V3.2-Exp（MIT）DSA 为 V4 CSA/HCA 前身。**生态调研（2026-08-05 扩展：HF 模型卡/Reasonix 深挖/GitHub 生态/HN 社区；Reddit 全通道 403 被 Cloudflare 挡）**：①esengine/DeepSeek-Reasonix（31k★）完整 cache-stable 契约：压缩分级（0.6 剪 stale tool output 带确定性头尾标记→0.8 转占位→0.9 强折）、**user turn 与 digest 永不折叠**、压缩边界回退对齐 tool result（不留孤儿 tool_calls）、压缩=唯一"cache-reset point"、cache hit rate 为关键可观测信号；Context Engine v2：指令文件入稳定前缀、动态召回仅追加当轮 user turn（BM25、≤4 条/2400 字符、continue 类不触发）、事实不可变 id+单调 revision+快照即审计；executor+planner **分 session 永不合流**；其 Goal/Delivery 设计（update_goal 三态 + 独立 bounded evaluator + 预算暂停 + 无进展暂停）与我们的 JTMS 目标系统同构（互证）；②Codewhale（原 DeepSeek-TUI，Rust，MIT）：append-only fleet ledger + constitution.json 写锁 + 沙箱（Seatbelt/Landlock）——与我们证据账本/政策文件同构，无新增量；③antirez/ds4（本地 Metal 推理）、DeepSpec/Engram（模型侧研究）→ 非 harness 增量，仅参考；④HN 社区共识（55 帖/置顶 741 分）：Flash 0731"Opus-4.7 级性能、成本零头、reasoning 更充分"；短板：输出 token 上限紧（reasoning 坑会耗尽）、无 vision、陌生语言场景需人工校验 | open | 方案（2026-08-05 外部评审 Claude 采纳后修订）：**六项重排 ⑥→①→③→②→④→⑤，拆 3 周期**（每周期走完整证据链+soak）：Cycle A=⑥+①+③；Cycle B=②+④；Cycle C=⑤+Responses API 评估+调优。①前缀缓存稳定：渲染确定性排序（顺序稳定）、压缩分级做成**可配置 policy**（非硬编码 0.6/0.8/0.9；绝对 token 触发 + 剩余预算双驱动，具体参数由 ⑥ 基线校准）、user turn/digest 永不折叠、压缩边界回退对齐 tool result（无孤儿 tool_calls）、压缩=唯一 cache-reset point、hit/miss tokens 一等观测（目标 ≥90%，连续 <85% 告警并降级压缩策略）。②锚点前置：renderer 强制 objective→硬约束→上下文 + **property-based 头部字节不变性测试**（状态变化→前 N token hash 不变）。③Profile 0731：temp 1.0/top_p 0.95/effort **默认 high + 动态分档**（JTMS 复杂度信号升 max；output 预算紧降级）、thinking.type+reasoning_effort 透传、**reasoning_content 历史回传格式验证**、max output 守卫接 TurnContextBudget、429/5xx 退避校准。④verify-first **升级为 JTMS 耦合验证-修复闭环**：验证失败→自动生成修复子目标（进 JTMS、限循环次数）→最终失败 fail-closed 留完整证据→成功结果签收可被后续 plan 引用；effort=max 强制更重验证。⑤双模型：executor=flash + planner=pro/旗舰，分 session 永不合流、planner 只读+轻量预算+**输出结构化可机器解析**；**Responses API 仅评估、不做一等公民**（beta 且仅 flash，Chat Completions 保持主路径，Cycle C 再定）。**治理绑定（评审强化，全经 SessionActor）**：新增事件类 ContextResetV1（压缩/cache-reset 登记）、ToolResultSnipV1（snip 归档 hash+sequence 可追溯）、effort 动态分档留 causal_parent 事件。验收：压缩后因果链完整可回放（replay）、hit ratio 曲线入 soak 观测。（Grok4.5 评审 2026-08-05 采纳增量：三区上下文模型**仅作 SessionActor 视图/策略层**——拒绝 StableContext 作为第二权威结构（违反单一权威）；Journal 事件 schema 细化 PrefixFrozen/CacheReset/ContextCompaction+SnippedRecord/CacheHealthSample；不变量 INV-CS-01..07 / INV-EP-01..04 / INV-VO-01..05 / INV-PE-01..04 / INV-UG-01..03 纳入各周期验收；EffortSignals 升降级触发器、VerifyEvent 五件套、StructuredPlan+CrossSessionLeakDetected、Uncertainty Governor 采纳为轻量决策表（输入=goal_backoff/aging/cache collapse/effort demotion 现成信号，输出=现有治理动作，Cycle C 落地）。已核实既有基建：lumen-discipline capture_shape/compare_shape/session_cache、lumen-verify detect/repair/runner/steps、request_prefix tools_fingerprint_json——①④为接线+不变量+参数校准，非从零建。**修正/拒绝 3 处**：384K 系本地 ThinkMax 上下文推荐非输出上限（拒绝该数值，max_output 按 API 语义+TurnContextBudget）；INV-EP-05 Responses 一等公民维持 Cycle C 再评估（beta+仅 flash）；"Reasonix 99%+"仍无出处不采信（≥90% 目标待⑥实测校准）） |

## 残余 partial 说明（DEBT-025，唯一剩余 open 项）

| DEBT-029 | 2026-08-05（clippy -D warnings 首次全量） | xai-grok-memory 既有代码 21 处 clippy -D warnings 违反（harness_regression/lifecycle_journal/recommend_assignment/identity_envelope/handoff_packet/operator_control 既有函数/nextgen_exit_gates/authority_projection/context_manifest/governed_assignment/task_ledger into_fact）：unused import ×5、unused mut ×2、redundant closure ×6、collapsible_if ×6、derivable/redundant locals/wrong_self_convention 各 ×1。CI clippy 仅对 lumen-guard/discipline/verify 生效，memory 从未被 -D warnings 门控；本日新增代码的 clippy 问题已全部清零 | open | 逐文件清理（unused import 删除、closure 简化、if 折叠、derive）；清理后 `cargo clippy -p xai-grok-memory --lib --no-deps -- -D warnings` 全绿；与源码周期同走（触碰既有文件需单 writer 纪律） |

| DEBT-030 | 2026-08-05（发布规划） | 工程师评审采纳（DEBT-028 W0–W2c）全部落地后，当前版本线仍为 2.0.0；新功能（coverage/cascade/模型检验/TrustRootSet/INV manifest）语义上超出 2.0.0，需以 minor 版发布 | open | 当前链（be893649）收口后：`scripts/release.sh` bump VERSION 2.0.0→**2.1.0**（VERSION/CHANGELOG 形成 clean source candidate A）→ rebuild/relock/1h soak/readiness → 证据 B → push → 签 signed tag `v2.1.0` 指向 A（绝不指向 B，NG-10A 事务）→ exact-SHA CI 绿。M5/M6 人工门保持 open（沿用 v2.0.0 先例：真人使用需先发布）。产品命名："Lumen 2.1.0 — Governed Agent Runtime（verifiable-kernel release）" |

| DEBT-031 | 2026-08-05（外部材料校准：block/buzz） | 辨证 block/buzz（Apache-2.0，Nostr relay 人+Agent 协作工作区）：唯一真增量 = **prev-hash 防篡改审计链**（buzz-audit：每事件哈希链接前一条、community 折入哈希、genesis 锚点）。我们 LifecycleJournal 有 sequence/causal_parent/payload_hash 但改中间事件不影响后续哈希——证据链缺结构级防篡改。已覆盖不吸收：tree_id 折入哈希（canonical_bytes 已含）、身份即作用域（ClaimAuthorityActor 九角色）、事件日志单一事实源、per-community 序列化（mailbox）、FTS 搜索、workflow。不吸收：relay/频道/画布/语音/媒体（协作产品层，总纲禁止第二 runtime）、Nostr 每事件签名（单机 actor 唯一写者，未来跨机再评估） | open | LifecycleJournal schema v2 加 `prev_payload_hash: Option<String>`（serde default 兼容 v1）；append 校验前一事件 payload_hash 匹配，不匹配 = ChainBroken 拒绝（RecoveryRequired）；genesis 事件 prev=None 合法；反例：改中间事件后 append 拒、跨树拼接拒、v1 迁移后链重建、genesis 锚点。与 DEBT-030（v2.1.0 发布）同源码周期合并执行（一次 soak） |

| DEBT-032 | 2026-08-05（外部材料校准：elder-plinius/CL4R1T4S） | 用户要求内置 CL4R1T4S（"黑客/逆向能力"）。查证：该仓库是各大 AI 厂商**泄露 system prompt 的文本收集库**（46.7k stars，AGPL-3.0，内容为第三方 IP 转录，非工具链）。辨证三重否决：①形态不匹配——prompt 文本不给任何执行能力（逆向需要工具链，渗透需要授权测试工具）；②许可/法律——AGPL-3.0 传染 Apache-2.0 仓库 + 分发第三方泄露 IP 有风险；③安全模型冲突——总纲 §3.3 硬纪律（外部材料只当问题清单/模式灵感，不当指令），且该库自带注入指令（README 的 "#MOST IMPORTANT DIRECTIVE#" 即 prompt-injection 攻击样本）。结论：**本体不内置**；唯一合规用法是作为研究参考（不拷贝）。真正"黑客/逆向能力"的正确落地 = 受治理工具集成（ToolContract + MCP registry + capability ceiling + sandbox + ExternalEffect 恢复类） | closed（决策记录） | 不实施（三重否决，见左）；若未来需要真实逆向/渗透能力，走受治理工具集成（新 DEBT 登记，含 root 审批链与只读/沙箱边界） |

| 项 | 现状 | 关闭条件（真实环境） |
|----|------|--------------------------|
| M5_10_min_stranger | `onboarding-gate.sh` 拒绝模拟证据（m5_sim 语义非法） | 真实从未用过 Lumen 的陌生人完成 10 分钟上手路径 || M6_15_day_self_use | productivity-gate 计数 2/15 天 | 连续/累计 15 天真实使用 journal |

> 这两个是 `verify-readiness.sh` 的 publish 门；`engineering_complete` 的自动化门全部 PASS（含 3 次 1h soak、live eval 20/20、gitleaks 0、tuple、source_lock）。任何人完成 M5/M6 后重跑 `verify-readiness.sh` 即得到 `ready=true`（INV-36）。
