# Lumen 2 — Harness Kernel local-ready 交接 + 人工门清单（2026-08-04）

**状态：** 自动化部分已全部闭合（S8/S9/S12/S10/S13/S14-local），本机达到交接词 §8 定义的 **"Harness Kernel local-ready"**。
**不宣称：** RC、正式 2.0、Windows、24h soak、无人值守、真实 provider live 证明。

## 自动化完成清单（agent 已做，均有证据）

| 切片 | 提交 | 证据 |
|------|------|------|
| S8 durable sealed receipt | `bf489044`/`1d2921af` | sealed 12、nextgen_control 22、s8 live 5、auth 30、provider 11、FULL_AUDIT 六路径 |
| S13 release 两段事务 + NG-10A + updater fail-closed | `88b1e490`/`407e5841`/`2efcea18` | test-release-order.sh 7/7、release_tuple 13、gate 10+8、throwaway 演练 |
| S9 Advisor consult + epoch 写侧 | `ca828123`/`af5c1882` | client_advisor 8、consult host 7、epoch 写侧测试、DEBT-001 closed |
| S12 OperatorControlPlane + lease-consumer | `1051fc2d`/`a97d4360` | operator 12、lease consumer 8、journal 集成（no-revival） |
| S10 五 corpus + verification_debt read model | `945d505a`/`979b4f0f` | harness_regression 24 场景、debt==0（10/10 closed）、exact-source binary 重建 |
| S14-local 三段式 | `4c06ed41`→`5138e8bd` | install-local + check-binary-tuple PASS（release==installed==locked==4c06ed41）+ gitleaks clean（SCRATCH/r0.log） |
| 全量回归 battery | `9864cc0d`/`5138e8bd` | 15 套件全绿含 pager 8003；SCRATCH/full-suite.log |

**CI 状态记录（2026-08-04 03:10）**：
- 旧 head（1c349210）run 30839923309：Expert v2 gate **PASS**；Offline gates 失败于 SOURCE_LOCK critical drift（根因：刷新锁未 push，已解决）；Lumen crates 失败于 Pager crate voice 测试 env 泄漏（已修复：`9864cc0d` 串行化 + env 隔离，本地串行/并行均绿）
- 新 head（5138e8bd）：PR #134 mergeStateStatus=CLEAN；CI 重跑检查排队中（按纪律：最终 exact-SHA 结论待 H1 人工签核确认）

## 人工门禁（agent 不能代做；做完才可称 RC）

| # | 门禁 | 剩余动作 | 谁 | 前提 |
|---|------|----------|-----|------|
| H1 | **exact-SHA CI 签核** | 对最终 HEAD 记录 `gh pr checks` 全绿（或按纪律写 NOT RUN）；PR #134 merge state 复核 | 用户/CI | 最终 HEAD 已 push |
| H2 | **PR/merge 分门** | PR #134（或新 PR）人工 review + merge 到 main | 用户 | H1 绿 |
| H3 | **`v2.0.0-rc.1` tag** | 在 **clean source A**（不是 evidence B）上打 signed tag；用修复后的 release.sh 两段事务 | 用户 | H2 后 |
| H4 | **§0.4 全场景验收** | 用 rebuilt exact-source binary 真跨 ACP/TUI seam 跑全部 golden path | 用户 | H3 后 |
| H5 | **grok-4.5 401 登录态根治** | x.ai 认证 401（reason=no auth context，OIDC token 16 分钟内被代理拒）根治在登录态/上游；客户端已能检测+指引（`failure_escalation_guidance`），根治需重新登录确认是否复现 | 用户/上游 | 独立于代码 |

**明确排除（交接词 §8）**：Windows 专项、真实 provider/billable 调用、24h soak、无人值守 daemon、正式 2.0 release。

## 收尾检查单（本次会话结束前已确认）

- [x] `git status -sb` 干净
- [x] 全部提交已 push（origin/sync/absorb-upstream-20260731）
- [x] SOURCE_LOCK 与 HEAD 一致（或 docs-only 差异）
- [x] 本机 binary 与 lock 一致（S14 三段式后）
- [x] 交接词/需求文档已更新（S15 需求、verification_debt、本清单）
- [x] 无未声明的红（全部 debt closed 或已列出）
