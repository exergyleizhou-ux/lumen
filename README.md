# Lumen

Coding agent monorepo: **Grok Build fork** as `agent/`, Lumen packs, Masterplan docs.

See `docs/masterplan/` for the execution plan. Day 0: get `agent/` compiling.

```bash
cd agent && cargo check -p xai-grok-pager-bin
```
